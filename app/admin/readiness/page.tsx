import { requireAdmin } from "../../../lib/supabase/auth";import { merchant,ownerInfoComplete,policyConfig } from "../../../lib/merchant";import { COMMERCE_ENABLED } from "../../../lib/commerce";export const dynamic="force-dynamic";export default async function Page(){const {role}=await requireAdmin();if(role!=="super_admin")return <main id="main-content" className="admin-page"><h1>Super-admin access required.</h1></main>;const checks=[["Business name",merchant.name],["Contact number",merchant.phone],["Support email",merchant.email],["Support hours",merchant.hours],["Business address",merchant.address],...[...Object.entries(policyConfig.shipping),...Object.entries(policyConfig.returns)].map(([name,value])=>[name.replaceAll(/([A-Z])/g," $1"),value])];const paymentReady=ownerInfoComplete&&Boolean(process.env.RAZORPAY_KEY_ID&&process.env.RAZORPAY_KEY_SECRET&&process.env.RAZORPAY_WEBHOOK_SECRET)&&!COMMERCE_ENABLED;return <main id="main-content" className="admin-page"><p className="eyebrow">Private super-admin review</p><h1>Store readiness.</h1><p>Commerce remains {COMMERCE_ENABLED?"enabled":"disabled"}. Razorpay activation is blocked until every owner field and server secret is present and the account is approved.</p><div className="admin-table">{checks.map(([label,value])=><div key={label}><strong>{label}</strong><span>{value?"Confirmed":"Owner input required"}</span></div>)}</div><section className="admin-empty"><h2>Razorpay checklist</h2><p>Server order pricing · payment signature · signed webhook · idempotent inventory update · success/cancellation/failure states · secret separation</p><strong>{paymentReady?"Technical review can proceed; explicit live approval is still required.":"Not ready for live activation."}</strong></section></main>}
