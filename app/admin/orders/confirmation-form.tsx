"use client";import {useState} from "react";export function ConfirmationForm(){const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/whatsapp-orders",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({reference:form.get("reference"),customerIdentifier:form.get("customerIdentifier")||undefined,couponCode:form.get("couponCode")||undefined,subtotalPaise:Number(form.get("subtotal"))*100,discountPaise:Number(form.get("discount")||0)*100})});const data=await response.json();setMessage(data.message);if(response.ok)event.currentTarget.reset();}return <form className="admin-form" onSubmit={submit}><label>Order reference<input name="reference" required /></label><label>Customer phone or email<input name="customerIdentifier" /></label><label>Coupon code<input name="couponCode" autoCapitalize="characters" /></label><label>Eligible subtotal (₹)<input name="subtotal" type="number" min="0" required /></label><label>Confirmed discount (₹)<input name="discount" type="number" min="0" defaultValue="0" /></label><button className="button button-dark" type="submit">Confirm WhatsApp order</button><p className="form-message" aria-live="polite">{message}</p></form>}
