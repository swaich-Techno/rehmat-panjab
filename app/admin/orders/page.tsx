import type {SupabaseClient} from "@supabase/supabase-js";import {requireAdmin} from "../../../lib/supabase/auth";import {ConfirmationForm} from "./confirmation-form";export const dynamic="force-dynamic";export default async function Page(){const {supabase,role}=await requireAdmin();if(role!=="super_admin")return <main id="main-content" className="admin-page"><h1>Super-admin access required.</h1></main>;const {data}=await (supabase as unknown as SupabaseClient).from("whatsapp_order_confirmations").select("id,reference,total_paise,status,created_at").order("created_at",{ascending:false}).limit(50);return <main id="main-content" className="admin-page"><p className="eyebrow">House administration</p><h1>Orders.</h1><ConfirmationForm/><div className="admin-table">{(data??[]).map(item=><div key={item.id}><strong>{item.reference}</strong><span>₹{(item.total_paise/100).toLocaleString("en-IN")}</span><span>{item.status}</span></div>)}</div></main>}
