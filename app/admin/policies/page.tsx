import type {SupabaseClient} from "@supabase/supabase-js";
import {requireAdmin} from "../../../lib/supabase/auth";
import {defaultStoreSettings,type StoreSettings} from "../../../lib/store-settings-schema";
import {PolicySettingsForm} from "./policy-settings-form";
export const dynamic="force-dynamic";
export default async function Page(){const {supabase,role}=await requireAdmin();if(role!=="super_admin")return <main id="main-content" className="admin-page"><h1>Super-admin access required.</h1></main>;const {data}=await (supabase as unknown as SupabaseClient).from("store_policy_settings").select("merchant,shipping,cancellation,returns,privacy,terms,publish_status,admin_notes,policy_version,policy_effective_date,support_email_verified_at,support_phone_confirmed_at,shipping_calculation_verified_at,courier_configuration_verified_at").eq("id",true).maybeSingle();const initial=data?{merchant:data.merchant||{},shipping:data.shipping||{},cancellation:data.cancellation||{},returns:data.returns||{},privacy:data.privacy||{},terms:data.terms||{}} as StoreSettings:defaultStoreSettings;const checks={email:Boolean(data?.support_email_verified_at),phone:Boolean(data?.support_phone_confirmed_at),shipping:Boolean(data?.shipping_calculation_verified_at),courier:Boolean(data?.courier_configuration_verified_at)};return <main id="main-content" className="admin-page"><p className="eyebrow">Private super-admin settings</p><h1>Policies.</h1><p>Structured fields feed version-controlled policy templates. Incomplete or operationally unverified drafts stay unpublished.</p><PolicySettingsForm initial={initial} published={data?.publish_status==="published"} notes={String(data?.admin_notes||"")} version={String(data?.policy_version||"2026-09-09.1")} effectiveDate={String(data?.policy_effective_date||"2026-09-09")} checks={checks}/></main>}
