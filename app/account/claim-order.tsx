"use client";
import {FormEvent,useState} from "react";
export function ClaimOrder(){const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const orderId=String(new FormData(event.currentTarget).get("orderId")??"").trim();const response=await fetch("/api/account/orders/claim",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({orderId})});const value=await response.json();setMessage(value.message);if(response.ok)window.location.assign("/account/orders");}return <form className="claim-order" onSubmit={submit}><h2>Claim a guest order</h2><p>Use the order ID only when its delivery email matches this verified account.</p><label htmlFor="claim-order-id">Order ID</label><input id="claim-order-id" name="orderId" inputMode="text" required/><button className="button button-outline" type="submit">Verify and claim</button><p role="status">{message}</p></form>;}
