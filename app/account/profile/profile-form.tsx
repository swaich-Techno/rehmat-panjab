"use client";
import {FormEvent,useState} from "react";
export function ProfileForm({email,displayName}:{email:string;displayName:string}){const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const displayName=String(new FormData(event.currentTarget).get("displayName")??"");const response=await fetch("/api/account/profile",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({displayName})});setMessage((await response.json()).message);}return <form className="account-form" onSubmit={submit}><label>Email<input value={email} disabled/></label><label>Display name<input name="displayName" defaultValue={displayName} minLength={1} maxLength={80} required/></label><button className="button button-dark" type="submit">Save profile</button><p role="status">{message}</p></form>;}
